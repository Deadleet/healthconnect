<div class="header">
  <div class= "header-title">
  <% if user_signed_in? %>
    <h1>Mon traitement</h1>
    <% end %>
  </div>
</div>
<div class = "container">
<h1><strong><%= @followup.title %></strong></h1>
<p><%= @followup.description %></p>
<p> Debut : <strong><%= @followup.start_date %></strong> - Fin : <strong><%= @followup.end_date %></strong></p>

<p>Posologie : <%= @followup.posology %>. Tous les <%= @followup.hourly_frequency %> jour(s), <%= @followup.day_frequency %> fois/jour</p>


  <div class ="my-2">
    <p><%= link_to "Modifier mon traitement", edit_prescription_follow_up_path(@prescription, @followup),class: "m-1 bouton d-flex justify-content-center" %></p>
    <p><%= link_to "Retour à l'ordonnance", prescription_path(@prescription),class: "m-1 bouton d-flex justify-content-center" %></p>
  </div>
</div>
