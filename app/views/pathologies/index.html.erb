<div class="header">
  <div class= "header-title">
  <% if user_signed_in? %>
    <h1>Bonjour, <%= current_user.first_name%></h1>
    <% end %>
  </div>
</div>
<h1>Pathologies</h1>


  <h4 class = "mt-2">Mes pathologies</h4>
  <ul class = "list-group">
    <% @pathologies.each do |pathologie| %>
      <li class = "list-group-item" ><p><%= pathologie.created_at %></p>
        <div class = "d-flex justify-content-between">
          <h4><%= pathologie.name %> <%= pathologie.description %></h4>
          <%# <%= link_to "modifier", ROUTE_path(pathologie), class: "btn-bi"  %>
      </li>
    <% end %>
  </ul>
