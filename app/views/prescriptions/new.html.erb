
   <div class="edit-form">
    <div class="container form">
    <div class="bg-white rounded p-3 shadow">
     <h3>Créer une Ordonnance</h3>
      <%= simple_form_for @prescription, data: { controller: 'flatpickr' } do |f| %>
          <div>
          <%= f.input :title, placeholder: 'Titre', value: "Titre", label: "Titre" %>
          <%= f.input :description, placeholder: 'Description'%>
          </div>

          <div>
          <%= f.input :start_date, label: "début", as: :string, input_html: { data: { flatpickr_target: "startTime" } },value: "Commence le" %>
          <%= f.input :end_date, label: "au", as: :string, input_html: { data: { flatpickr_target: "endTime" }, id: 'end_time' },value: "Fini le" %>
         </div>

          <div>
          <%= f.input :practician, placeholder: 'Le nom du Practicien',label: "Praticien"%>
          <%= f.submit 'Créer', class: "bouton" %>
          </div>

      <% end %>
    </div>
  </div>
</div>
